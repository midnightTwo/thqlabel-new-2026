# Настройка системы ответов в тикетах

## Шаг 1: Выполните SQL миграцию

Выполните файл `sql/add_reply_to_messages.sql` в вашей Supabase базе данных:

1. Откройте Supabase Dashboard
2. Перейдите в SQL Editor
3. Скопируйте содержимое файла `sql/add_reply_to_messages.sql`
4. Вставьте и выполните запрос

## Что добавляется:

```sql
-- Добавление поля reply_to для системы ответов на сообщения в тикетах

ALTER TABLE ticket_messages
ADD COLUMN IF NOT EXISTS reply_to UUID REFERENCES ticket_messages(id) ON DELETE SET NULL;

CREATE INDEX IF NOT EXISTS idx_ticket_messages_reply_to ON ticket_messages(reply_to);

COMMENT ON COLUMN ticket_messages.reply_to IS 'ID сообщения на которое отвечает данное сообщение';
```

## Шаг 2: Проверьте работу

После выполнения миграции:

1. **В виджете пользователя** (`/cabinet`):
   - Наведите мышь на любое сообщение в тикете
   - Появится кнопка "Ответить" с иконкой стрелки
   - Нажмите на неё
   - Появится превью сообщения над полем ввода
   - Напишите ответ и отправьте
   - В новом сообщении будет показана ссылка на исходное

2. **В админ-панели** (`/admin`):
   - То же самое - наведение, кнопка ответа, превью
   - Ответы работают в обе стороны (админ → пользователь, пользователь → админ)

## Особенности:

- ❤️ **Лайки** отображаются под сообщением (двойной клик или кнопка)
- 💬 **Ответы** при наведении справа/слева от сообщения
- 🔗 **Превью ответа** в синей рамке внутри сообщения
- 🗑️ **Отмена ответа** - крестик в превью над полем ввода

## Структура данных:

После миграции поле `reply_to` в таблице `ticket_messages`:
- Тип: `UUID` (ссылка на другое сообщение)
- Nullable: `true` (не обязательное)
- Foreign Key: `ticket_messages(id)` с `ON DELETE SET NULL`
- Индекс: `idx_ticket_messages_reply_to` для быстрых запросов

API автоматически загружает данные о связанных сообщениях при получении тикета.
