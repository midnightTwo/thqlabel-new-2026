# 🎯 Функционал Лейбла и Аудиоплеер - Руководство по установке

## 📋 Содержание

1. [Установка зависимостей](#установка-зависимостей)
2. [Миграция базы данных](#миграция-базы-данных)
3. [Функционал лейбла](#функционал-лейбла)
4. [Аудиоплеер](#аудиоплеер)
5. [Проверка работы](#проверка-работы)

---

## 📦 Установка зависимостей

### 1. Установите библиотеку для работы с Excel

```bash
npm install exceljs
```

---

## 🗄️ Миграция базы данных

### Шаг 1: Добавьте поля каталожного номера и копирайта

Откройте **Supabase SQL Editor** и выполните:

```sql
-- Файл: sql/add_catalog_number_and_copyright.sql
```

Этот скрипт добавит:
- ✅ Поле `catalog_number` (формат: REL-XXX)
- ✅ Поля `phonographic_copyright` (℗) и `copyright_text` (©)
- ✅ Автоматическую генерацию каталожного номера при публикации
- ✅ Автозаполнение копирайта (формат: "2025 thqlabel")
- ✅ Принудительный lowercase для названия лейбла

### Шаг 2: Присвойте номера существующим релизам

После выполнения первого скрипта выполните:

```sql
-- Файл: sql/migrate_assign_catalog_numbers.sql
```

Этот скрипт:
- ✅ Присвоит каталожные номера всем опубликованным релизам
- ✅ Установит копирайт для старых релизов
- ✅ Покажет статистику выполнения

---

## 🏷️ Функционал Лейбла

### Каталожный номер (Catalog Number)

**Формат:** REL-XXX (например: REL-001, REL-002)

**Автоматическая генерация:**
- Номер присваивается автоматически при переходе релиза в статус `published`
- Нумерация сквозная для обеих таблиц (releases_basic и releases_exclusive)
- Каждый номер уникален

**Ручное присвоение (если нужно):**
```sql
UPDATE releases_basic 
SET catalog_number = generate_catalog_number()
WHERE id = 'release_id' AND catalog_number IS NULL;
```

### Копирайт (Copyright)

**Автоматические значения:**
- ℗ (Phonographic): `2025 thqlabel`
- © (Copyright): `2025 thqlabel`

**Особенности:**
- Год берется текущий
- Название лейбла принудительно в lowercase
- Значения устанавливаются при создании/обновлении релиза

**Изменение копирайта:**
```sql
UPDATE releases_basic 
SET 
  phonographic_copyright = '2025 custom label',
  copyright_text = '2025 custom label'
WHERE id = 'release_id';
```

### Excel-отчеты метаданных

**Где найти:**
- В админ-панели откройте любой релиз
- Кнопка **"Скачать метаданные (.xlsx)"** в правом блоке с действиями
- Доступна при любом статусе релиза

**Структура файла:**

| Catalog Number | Track Number | Filename | Track Title | Artist | Composer | Lyricist | Producer | Featuring | ISRC | Language | Instrumental |
|----------------|--------------|----------|-------------|--------|----------|----------|----------|-----------|------|----------|--------------|
| REL-001 | 01 | 01_Artist_Track.wav | Track Name | Artist Name | John Doe | Jane Doe | Producer X | Feat. Y | USRC17607839 | Russian | No |

**Формат имен файлов:**
- Шаблон: `{TrackNumber}_{Artist}_{TrackTitle}.wav`
- Пример: `01_Артист_Название_Трека.wav`

**Дополнительная информация:**
В конце файла добавляется секция с общей информацией о релизе:
- Название
- Артист
- Каталожный номер
- UPC код
- Жанр
- Дата релиза
- Копирайты (℗ и ©)
- Тип (Basic/Exclusive)
- Статус

---

## 🎵 Аудиоплеер

### API для защищенного стриминга

**Endpoint:** `/api/stream-audio`

**Параметры:**
- `releaseId` - ID релиза
- `releaseType` - тип релиза (`basic` или `exclusive`)
- `trackIndex` - индекс трека (начиная с 0)

**Авторизация:**
- Передается через заголовок `Authorization: Bearer {token}`
- Админы и владельцы релизов могут слушать всегда
- Остальные пользователи - только опубликованные релизы (`published`)

**Пример использования:**
```javascript
const response = await fetch(
  `/api/stream-audio?releaseId=${id}&releaseType=basic&trackIndex=0`,
  {
    headers: {
      'Authorization': `Bearer ${userToken}`
    }
  }
);
```

### Админ-панель

**Что изменилось:**
- ✅ Кнопка "Прослушать" заменена на встроенный плеер
- ✅ Функции: Play/Pause + полоса прогресса
- ✅ Показ текущего времени и общей длительности
- ✅ Перемотка с помощью слайдера

**Где найти:**
1. Откройте админ-панель
2. Перейдите в "Релизы"
3. Откройте любой релиз
4. Разверните трек в списке
5. Плеер отображается вместо кнопки "Прослушать"

### Кабинет пользователя

**Что изменилось:**
- ✅ Квадраты с номерами треков теперь кнопки Play/Pause
- ✅ Активен только для статусов `published` и `distributed`
- ✅ Для других статусов показывается обычный номер трека

**Где найти:**
1. Откройте личный кабинет
2. Перейдите в "Мои релизы"
3. Откройте опубликованный или на дистрибьюции релиз
4. Нажмите на номер трека для воспроизведения

**Варианты плеера:**

1. **Компактный** (в списке треков):
   - Квадратная кнопка с иконкой Play/Pause
   - При клике начинается воспроизведение

2. **Полный** (в админке):
   - Кнопка Play/Pause
   - Полоса прогресса с возможностью перемотки
   - Отображение времени

---

## ✅ Проверка работы

### 1. Проверьте поля в базе данных

```sql
-- Проверка структуры releases_basic
SELECT 
  column_name, 
  data_type, 
  is_nullable
FROM information_schema.columns
WHERE table_name = 'releases_basic' 
  AND column_name IN ('catalog_number', 'phonographic_copyright', 'copyright_text');

-- То же для releases_exclusive
SELECT 
  column_name, 
  data_type, 
  is_nullable
FROM information_schema.columns
WHERE table_name = 'releases_exclusive' 
  AND column_name IN ('catalog_number', 'phonographic_copyright', 'copyright_text');
```

### 2. Проверьте функцию генерации номера

```sql
SELECT generate_catalog_number();
-- Должно вернуть следующий доступный номер, например: REL-042
```

### 3. Тестирование каталожного номера

```sql
-- Создайте тестовый релиз и опубликуйте его
INSERT INTO releases_basic (user_id, title, artist_name, genre, status)
VALUES (
  'your-user-id',
  'Тестовый релиз',
  'Тестовый артист',
  'Pop',
  'pending'
);

-- Обновите статус на published
UPDATE releases_basic 
SET status = 'published'
WHERE title = 'Тестовый релиз';

-- Проверьте, что номер присвоился
SELECT catalog_number, phonographic_copyright, copyright_text
FROM releases_basic
WHERE title = 'Тестовый релиз';
```

### 4. Тестирование Excel экспорта

1. Откройте админ-панель
2. Перейдите к любому релизу
3. Нажмите "Скачать метаданные (.xlsx)"
4. Проверьте содержимое файла

### 5. Тестирование аудиоплеера

**В админке:**
1. Откройте любой релиз с треками
2. Разверните трек
3. Нажмите на кнопку Play
4. Проверьте работу полосы прогресса

**В кабинете пользователя:**
1. Откройте опубликованный релиз
2. Кликните на квадрат с номером трека
3. Должно начаться воспроизведение

---

## 🔧 Возможные проблемы и решения

### Проблема: Номер не присваивается при публикации

**Решение:**
```sql
-- Проверьте существование триггера
SELECT trigger_name, event_manipulation, event_object_table 
FROM information_schema.triggers 
WHERE trigger_name LIKE '%catalog%';

-- Если триггера нет, пересоздайте его из файла add_catalog_number_and_copyright.sql
```

### Проблема: Excel файл не скачивается

**Решение:**
1. Проверьте наличие переменной окружения `SUPABASE_SERVICE_ROLE_KEY`
2. Убедитесь что библиотека `exceljs` установлена
3. Проверьте консоль браузера на ошибки

### Проблема: Аудио не воспроизводится

**Решение:**
1. Проверьте что релиз опубликован (`status = 'published'`)
2. Убедитесь что у трека есть поле `link`
3. Проверьте права доступа к Storage бакету в Supabase
4. Откройте консоль браузера и проверьте ошибки сети

### Проблема: Копирайт не в lowercase

**Решение:**
```sql
-- Принудительное приведение к lowercase
UPDATE releases_basic 
SET 
  phonographic_copyright = LOWER(phonographic_copyright),
  copyright_text = LOWER(copyright_text);

UPDATE releases_exclusive 
SET 
  phonographic_copyright = LOWER(phonographic_copyright),
  copyright_text = LOWER(copyright_text);
```

---

## 📝 Примечания

- Каталожные номера присваиваются **только** при публикации
- Копирайт устанавливается **при создании** или **обновлении** релиза
- Excel экспорт доступен **всегда**, независимо от статуса
- Аудиоплеер работает **только** для published и distributed статусов
- Для воспроизведения нужна авторизация через Supabase

---

## 🚀 Что дальше?

После установки и проверки:

1. ✅ Проверьте работу на тестовых релизах
2. ✅ Создайте резервную копию базы данных
3. ✅ Запустите миграцию для присвоения номеров старым релизам
4. ✅ Проинформируйте пользователей о новом функционале
5. ✅ Настройте мониторинг ошибок API

---

Успехов! 🎉
